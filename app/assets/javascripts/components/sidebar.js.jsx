(function (root) {
  root.Sidebar = React.createClass({
    contextTypes: {
      router: React.PropTypes.func
    },

    render: function() {
      return(
      <div>

      </div>
      );
    }
  });
})(this);
